/** BehaviorSystem — computes and accumulates steering forces for all creatures.
 *  Stub for Phase 05 dependency resolution; fully implemented in Phase 09. */

import type { Tank } from '@/tank/Tank';

export class BehaviorSystem {
  /** Called once per fixed timestep. Applies forces to creature bodies. */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  update(_tank: Tank, _dt: number): void {
    // Implemented in Phase 09
  }
}
